package terminaison;

import java.util.concurrent.Semaphore;

public class ProdConsBuffer {
	Semaphore notEmpty;
	Semaphore notFull;
	Semaphore mutex;
	
	public void Produce(Message msg) {
		notFull.acquire();
		
		notEmpty.release();
	}
}
